# Compiled training experiment
experiment:
  report_dir: reports/example_compiled

compile: true

input_dim: 784
hidden_dim: 512
output_dim: 10

batch_size: 128
train_epochs: 10
learning_rate: 3.0e-4
